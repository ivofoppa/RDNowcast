% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NowcastProcessed.R
\name{NowcastProcessed}
\alias{NowcastProcessed}
\title{Helper function for the function \code{RDNowcast::Nowcast()}}
\usage{
NowcastProcessed(
  data,
  dateAnal = NULL,
  offset = 4,
  NCdates = NULL,
  NCsize = 10,
  reference_date = "reference_date",
  report_date = "report_date",
  unit = "week",
  nsamples = 1e+05,
  probs = c(0.025, 0.975),
  fd_distance = 20,
  NCperiods = 52,
  cnames = c("median", "lowerCrI", "upperCrI", "observed"),
  tu_lab = "week"
)
}
\arguments{
\item{data}{A data frame containing data, with one column representing event times and one with reporting times.
The data should be complete in the sense, that reporting delays no longer play a role.}

\item{dateAnal}{A date, represented in the format "YYYY-mm-dd" that sets the date of analysis; if missing, the most recent report_date + 1 is chosen.}

\item{offset}{The number of days before dateAnal of the most recent reference date considered.}

\item{NCdates}{A date vector, representing the dates used for the completeness estimation; these dates have to correspond to the same day of week as dateAnal.}

\item{NCsize}{An integer, representing the length of the time series in chosen time units, for which completeness estimates should be obtained.}

\item{reference_date}{A string variable representing the name of the column with event dates.}

\item{report_date}{A string variable representing the name of the column with reporting dates.}

\item{unit}{String representing time unit of analysis, week ("week") vs. day ("day").}

\item{nsamples}{An integer of default 100,000, representing the number of samples from the posterior distributions.}

\item{probs}{A numeric vector 0-1, representing the desired quantiles of the nowcast MCMC samples, besides the median.}

\item{fd_distance}{Interger, the number of weeks after data are assumed to be complete. Only used if NCdates is not assigned.}

\item{NCperiods}{Integer, the number of data sets used for completeness estimation. Only used if NCdates not assigned.}

\item{cnames}{A character vector with the desired names of the output table; the default values are \code{c(unit,"median","lowerCrI","upperCrI","observed")}, representing
time (in chosen time units), the median of the MCMC samples, the lower credible interval (level chosen in the argument \code{probs}), the upper credible interval and the observed counts.}

\item{tu_lab}{Desired label of time units in table.}

\item{NCperiod}{The numbers of weeks used for data generation. By default 52 weeks.}

\item{day_anal}{Integer representing the day of the analysis, with 1 (sunday) to 7 (saturday); the most recent reporting date is assumed
to be on day_anal - 1.}
}
\value{
A table with quantiles of the MCMC samples per date.
}
\description{
This helper function processes the draws from the imputed full numbers (without reporting delay), i.e. the posterior 
distributions for the full data.
}
\examples{
library(RDNowcast)

NCdata <- RDNowcast::RDdata  ### Loading the data set from the package

dateAnal <- as.Date("2026-05-13")  ### Setting the date of the analysis; the most recent data were reported one day prior

NCdates <- NCdates_create(data = NCdata, dateAnal = dateAnal, NCperiods = 5)

NCproc <- NowcastProcessed(data = NCdata, dateAnal = dateAnal, offset = 4, NCsize = 10 ,unit = "week")

NCproc
}
